{{ include('components/head.html.twig') }}
<body>
<h1>{{ heading }}</h1>
<p>{{ content }}</p>

{{ form_start(form) }}
{{ form_widget(form) }}

<input type="submit">
{{ form_end(form) }}

</body>
</html>