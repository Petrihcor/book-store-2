{{ include('components/head.html.twig') }}
<h1>{{ post.name }}</h1>
<p>{{ post.user }}</p>
<p>{{ post.category }}</p>
<p>{{ post.content }}</p>
    <p>{{ post.createData }}</p>
<p>{{ post.updateData }}</p>


</body>
</html>