{{ include('components/head.html.twig') }}
<div class="container">
    <h1>{{ heading }}</h1>
    <p>{{ content }}</p>

    {{ form_start(form) }}
    {{ form_widget(form) }}


    {{ form_end(form) }}
</div>
</body>
</html>